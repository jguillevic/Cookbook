namespace Tools.DAL.QueryBuilder.Enum
{
    /// <summary>
    /// Représente les opérateurs de tri pour les requêtes SELECT.
    /// </summary>
    public enum Sorting
    {
        Ascending,
        Descending
    }
}