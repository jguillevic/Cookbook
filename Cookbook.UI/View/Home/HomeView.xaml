<UserControl
    x:Class="Cookbook.UI.View.Home.HomeView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid>
        <CommandBar>
            <AppBarButton Label="Ingrédients" Command="{Binding GoToListIngredientsCommand}"/>
            <AppBarButton Label="Mesures" Command="{Binding GoToListMeasuresCommand}"/>
            <AppBarButton Label="Recettes" Command="{Binding GoToListRecipesCommand}"/>
            <AppBarButton Label="Repas"/>
        </CommandBar>
    </Grid>
</UserControl>
